fun main() {
    // var for variables that can be used for iterations (changing variables)
    // Don't use val for variables that needs to be changed like the 'i' below
    var i: Int = 0
    
    // do while loop
    do {
        println(i)
        i++
    } while (i < 10)
}
